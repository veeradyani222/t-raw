.venv/
__pycache__/
*.pyc
.env
trader.log
.pytest_cache/

# Large local-only data + third-party reference EAs — not published
data/
ea-library/

# Regenerable backtest outputs
logs/
backtest-orb-*.csv
xauusd-*.csv

# The box publishes its own state here (ops/daily_export.ps1). These are the
# committed COPIES — the root trader.log and the logs/ dir stay ignored.
!ops/state/
!ops/state/trader.log
!ops/state/watchdog.log
